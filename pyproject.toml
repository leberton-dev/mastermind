[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
	"pyright>=1.1.411",
	"pytest>=8.0",
	"ruff>=0.6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
